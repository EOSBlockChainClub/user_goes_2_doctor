const canDrive = plainData => {
  return true;
};
const canFly = plainData => {
  return false;
};
const canInsurance = plainData => {
  return true;
};
const canGym = plainData => {
  return true;
};
const canMarathon = plainData => {
  return false;
};

export { canDrive, canFly, canInsurance, canGym, canMarathon };
